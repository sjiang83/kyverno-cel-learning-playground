# Examples (CEL)

I’m collecting small, runnable CEL policy examples here.

Each example should include:
- the policy YAML
- a minimal resource YAML to test against
- the expected result (pass/fail + short note)
- the exact command(s) used to run it (so others can reproduce)
